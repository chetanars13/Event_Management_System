<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendees</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 py-4">
            <a href="/events" class="text-xl font-bold text-gray-900 hover:text-blue-600">
                Event Management
            </a>
        </div>
    </nav>


    <div class="px-4 sm:px-6 lg:px-8">
        <div class="sm:flex sm:items-center">
            <div class="sm:flex-auto">
                <h1 class="text-2xl font-semibold text-gray-900">Attendees</h1>
            </div>
            <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
                <a href="/attendees/new"
                    class="inline-flex items-center justify-center rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-blue-700">
                    Add Attendee
                </a>
                <a href="/tasks"
                class="inline-flex items-center justify-center rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-blue-700">
                View tasks
            </a>
            </div>
        </div>
        <!-- Attendees Table -->
        <div class="mt-8">
            <table class="min-w-full divide-y divide-gray-200 bg-white shadow-md rounded-lg overflow-hidden">
                <thead class="bg-gray-100">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">#</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Role</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                        
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <% attendees.forEach((attendee, index)=> { %>
                        <tr class="hover:bg-gray-100">
                            <td class="px-6 py-4 text-sm text-gray-900">
                                <%= index + 1 %>
                            </td>
                            <td class="px-6 py-4 text-sm text-gray-900">
                                <%= attendee.name %>
                            </td>
                            <td class="px-6 py-4 text-sm text-gray-500">
                                <%= attendee.email %>
                            </td>
                            <td class="px-6 py-4">
                                <span
                                    class="inline-flex px-2 text-xs font-semibold leading-5 rounded-full
                                    <%= attendee.role === 'Organizer' ? 'bg-blue-100 text-blue-800' : 'bg-green-100 text-green-800' %>">
                                    <%= attendee.role %>
                                </span>
                            </td>
                            <td class="px-6 py-4">
                                <a href="/attendees/<%= attendee._id %>/edit" class="text-blue-600 hover:text-blue-900 font-medium text-sm">Edit</a>
                                |
                                <form action="/attendees/<%= attendee._id %>?_method=DELETE" method="POST" class="inline">
                                    <button type="submit" class="text-red-600 hover:text-red-900 font-medium text-sm">
                                        Delete
                                    </button>
                                </form>
                            </td>

                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>

    </div>
</body>

</html>